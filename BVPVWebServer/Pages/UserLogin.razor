@page "/userlogin"

<PageTitle>Login to NPDES</PageTitle>

@inject NavigationManager _navMgr
@inject StateService _state
@{ _state.IsLoggedIn = false; }
@{
    if (_state.IsLoggedIn)
    {
        _navMgr.NavigateTo("/");
    }
}

